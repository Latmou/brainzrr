services:
  invidious:
    ports:
      - "${INVIDIOUS_PORT:-3010}:3000"

  brainzrr-db:
    ports:
      - "5432:5432"

  musicbrainz:
    ports:
      - "${MUSICBRAINZ_PORT:-5000}:5000"

  musicbrainz-db:
    ports:
      - "5433:5432"
